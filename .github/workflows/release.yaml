name: 'release'

on:
  push:
    branches:
      - 'main'

permissions:
  contents: 'write'

jobs:
  release:
    uses: rios0rios0/pipelines/.github/workflows/release.yaml@5769539004dc88fdbcc92afe44d0e6a8561b6668 # main
